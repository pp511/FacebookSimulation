/*

class CreateUser extends Actor{
  def receive = {
    case CreateUsers() => {

    }

  }
}
class AddFriend extends Actor{
  def receive = {
    case CreateUsers() => {

    }

  }
}
class ViewFriendlist extends Actor{
  def receive = {
    case CreateUsers() => {

    }
  }
}
class DeleteFriend extends Actor{
  def receive = {
    case CreateUsers() => {

    }

  }
}
class ViewPost extends Actor{
  def receive = {
    case CreateUsers() => {

    }

  }
}
class DeletePost extends Actor{
  def receive = {
    case CreateUsers() => {

    }

  }
}
class ViewPosts extends Actor{
  def receive = {
    case CreateUsers() => {

    }

  }
}*/

/*val createuser = system.actorOf(Props(new CreateUser()))
  val addfriend = system.actorOf(Props(new addfriend()))
  val deletefriend = system.actorOf(Props(new DeleteFriend()))
  val viewpost = system.actorOf(Props(new ViewPost()))
  val deletepost = system.actorOf(Props(new DeletePost()))
  val viewfriendlist = system.actorOf(Props(new ViewFriendlist()))
  val viewposts = system.actorOf(Props(new ViewPosts()))*/